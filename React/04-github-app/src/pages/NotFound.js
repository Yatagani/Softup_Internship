const divStyle = {
  margin: "3rem auto",
  textAlign: "center",
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
};

const NotFound = () => {
  return (
    <div style={divStyle}>
      <p>Page not found</p>
    </div>
  );
};

export default NotFound;
